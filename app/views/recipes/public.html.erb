<main class='container justify-content-center pt-4'>
  <h1 class='text-center mb-5'>Public Recipes</h1>

<div class='border p-3 rounded shadow'>
    <% @public_recipes.each do |recipe| %>
      <%= render 'recipe_card', recipe: recipe %>
    <% end %>
  </div>
</main>